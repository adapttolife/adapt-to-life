node_modules/
.wrangler/
.dev.vars
*.log
.DS_Store
public/build.txt

# Mission Control build output — produced by `npm run build:app` at deploy
# time (wrangler build.command), so committing it would only create churn and
# merge conflicts on generated bytes.
public/admin/app/
__pycache__/
